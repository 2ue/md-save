重新规划，我希望实现这样一个插件：
1.能够快速复制或者保存网页标题和url到本地/远端指定目录/文件
2.能否快速摘录网页，全网页一键摘录（需要考虑网页懒加载，选择合适的方式来处理），局部摘录（可多段选择），保存到本地或远端指定目录/文件
3.保存时希望提供全面的设置，比如展示最终存储的位置（包含目录），可以修改标题，内容，打标签，分类，修改内容，预览内容等等
4.设置页面需要支持设置保存目录（本地/远端），数据同步，导入导出等（需要你完善补充）
5.远端方式暂时只支持webdav
6.支持图片下载，两种方式，一键下载网页的全部的图片，也可以根据选择的内容中包含的图片来下载，可以在保存时静默在后台下载
7.自主选择下载位置（意思是在下载的时候来决定保存到哪里，设置里面仅仅是提供设置不同保存方式的配置）
8.需要保存摘录历史，记录到本地或远端，支持远端和本地数据合并
9.支持配置同步，考虑自动同步（浏览器插件限制貌似自动同步存在一些局限？）
...

根据以上需求来进行分析，我想实现一个完整的网页工具应用，主要是进行一些摘录保存工作，目前仅支持本地和webdav，后续希望支持一些其他渠道，比如github，notion等（但现在不考虑这些渠道，只考虑拓展性）
请参考一些现有竞品分析和一些常态化交互，逻辑等实现来进行分析，然后给出一些更全需求、功能、交互，ASSIC基本UI图等内容,和一些关键的方案（比如能否实现自动同步？是否存在局限？）能否实现？
请根据分析输出合适的文档，文档拆分为合理的结构，保持多文档方式，防止文档过长，文档需要输出到本地
分析过程中可以使用github，exa，context7等工具来获取信息，不要一次性获取大量信息，防止消息tokens过长
分析可以参考当前项目的一些实现，但是不是对当前项目的分析，仅仅是分析这些能力如何实现，后续进行全新的实现 ultrathink

提示词：https://linux.do/t/topic/1167907